# Checks participants can understand

Include fast checks that do not need hardware plus a separately labeled real platform smoke test. Each check must say what passing proves and does not prove. Mock tests never establish hardware readiness.

Author replaces this with: {{TEST_COMMANDS_AND_EXPECTED_RESULTS}}.

